<!--Navbar-->
<nav id="topo" class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center gap-2" href="#">
            <img src="<%= image_path('logo.png') %>" alt="Logo" style="width: 50px; height: 50px;">
            <span>Beleza Pura</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#services">Serviços</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#about">Sobre Nós</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#contact">Contato</a>
                </li>
                <% if logged_in? %>
                    <% if current_user.admin? %>
                        <li class="nav-item">
                            <%= link_to "Painel Admin" , admin_dashboard_path, class: "nav-link" %>
                        </li>
                        <% else %>
                            <li class="nav-item">
                                <%= link_to "Área do Cliente" , customers_dashboard_path, class: "nav-link" %>
                            </li>
                            <% end %>
                                <li class="nav-item">
                                    <%= link_to "Sair" , logout_path, method: :delete, class: "nav-link" %>
                                </li>
                                <% else %>
                                    <li class="nav-item">
                                        <%= link_to "Login" , login_path, class: "nav-link text-light" %>
                                    </li>
                                    <li class="nav-item">
                                        <%= link_to "Cadastre-se" , new_user_path,
                                            class: "nav-link text-light cadastro-link" %>
                                    </li>
                                    <% end %>

            </ul>
        </div>
    </div>
</nav>